# Session ledger — 2026-07-12

## ORCHESTRATOR
- CLAIM 15:50 — establish the multi-session protocol (CLAUDE.md section + _sessions/ convention)
- DONE 15:50 — files: CLAUDE.md, _sessions/README.md, _sessions/2026-07-12.md · verified: docs-only, no analyze needed · dirty: none

<!-- Other live sessions: claim your territory below before your next edit. -->

## party/items-market
- CLAIM 15:55 — the 22-item market catalog (Brett, yumutsu'd): always-open rarity shelf,
  13 new PowerUps + effects, gamePick phase, toll/stakes/warp mechanics. Files:
  party_models.dart, party_controller.dart (market/items/roll sections only),
  party_actions.dart, party_net.dart (pickMiniGame case), docs/ITEMS_SPEC.md.
- REQUEST — party_page.dart: shop panel (_shopOffer) reworked to the rarity shelf,
  targeted-item picker on the turn panel, gamePick phase screen, autopilot + phase-switch
  cases for PartyPhase.gamePick. Feature was assigned to this session directly by Brett
  pre-protocol; applying these party_page edits here (shop/turn-panel region only) unless
  a live orchestrator objects — flagged for the integration QA pass.
- NOTE — party_controller.dart is being co-edited by another live session (rules revision
  field + log casing). My edits are additive/surgical; integration analyze must wait for
  both to land.
- REQUEST — CLAUDE.md directory map: add `docs/ITEMS_SPEC.md — the 22-item market
  catalog (rarity shelf) — LOCKED spec` alongside MAPS_SPEC.md.
- DONE 16:40 — files: party_models.dart (13 appended PowerUps, ItemRarity, catalog +
  kCatalogPrices, player armed-state), party_controller.dart (bazaar flag + 'bazaar' save
  key, always-open shelf market w/ multi-buy, roll modifiers, toll/rigger/stakes/warp,
  gamePick phase + pickMiniGame input), party_actions.dart, party_net.dart (pickMiniGame
  routing), party_page.dart (shelf UI, target picker, _GamePickScreen, autopilot cases),
  docs/ITEMS_SPEC.md, test/party/items_catalog_test.dart (19 tests) + gamePick cases in 4
  existing test switches + party_controller_test walkOut helper (buy-then-leave).
  · verified: analyze clean in party scope; items_catalog (19) + party_controller (21) +
  board_play/pacing/round_ceremony suites pass. NOT mine, still red at my last run:
  ghost_flow "release is announced" + wheel_flow "FROZEN" (both expect SHOUTED log strings
  the casing-sweep session de-shouted) and prowl_economy ×2 (rules-v4 session, in flight).
  · dirty: none in my territory.

## party (controller/models/ceremony/wheel + their tests)
- CLAIM 15:50 — winner-pays law: mini-game awards → 10/5/1/0, and round winners are never fed a sting wheel (winner-table spin on checkpoint rounds, excluded from checkpoint queue); gated as rules rev 3 ('r' in saves) so old logs replay unchanged
- REQUEST — docs/PARTY_CINEMATIC_SPEC.md: update §2 wheel table + §4 + "Map dials" (line ~186): (a) Checkpoint row "every player" → "every player EXCEPT the round's winners (rules ≥3 — winning never exposes you to the sting table)"; (b) into_the_void dial: still no per-round winner spins, but on checkpoint rounds the round winner spins the winner table instead of the checkpoint table; (c) note mini-game diamond awards are 10/5/1/0 (nothing past third)

## party (controller + models + wheel/ceremony screens)
- CLAIM 16:05 — rules v4 economy: kill every-round ops leader-skim; ops become positional prowl-window threats (every 4th round, chance-based, avoidable); Big Bad wheel round every 7th round (last-loses-potato / diamond redistribution). Includes surgical party_page.dart edits (prowl visuals, intro stakes banner) — no other live session in ledger.

## party (controller walk/landing section + party_controller_test)
- CLAIM — Shack clock-out (Brett, live bug report: "stuck at the end of down_the_hole"):
  after clocking in at the anchor (+1 potato), the walker returns to square one on the
  COMPLETE TURN tap. Gated rules ≥ 4. Files: party_controller.dart (kPartyRules 3→4, rules
  doc, _clockOutPending field, _finishStep clock-in block + panel line, confirmSpace),
  party_controller_test.dart (new "Potato Shack (map anchor)" group).
- NOTE — rules-revision COLLISION with the 16:05 claim above: both sessions gate as rev 4.
  Fine if we land together (rev 4 = clock-out + prowl economy), but the `rules` doc comment
  in party_controller.dart must list BOTH; I bumped kPartyRules to 4 already — 16:05
  session: don't double-bump, append your semantics to the rev-4 doc entry.
- REQUEST — docs/MAPS_SPEC.md (orchestrator): amend THE POTATO SHACK section: "Clocking
  out (amended 2026-07-12, Brett): after clocking in, the player returns to square one
  (order 0) — the Shack is a checkpoint, not a parking spot. All three maps."
- DONE 16:20 — verified: analyze clean on party_controller.dart + party_controller_test.dart;
  both new Shack tests pass; full file 20/21 — the one failure is "buying a potato" (bazaar
  session's known in-flight walkOut/perusing mismatch, flagged above, not reachable by this
  diff) · dirty: none of mine · MAPS_SPEC.md REQUEST above still open.
- DONE 16:12 — files: lib/party/party_controller.dart (rules rev 3: _ffaAwardsV3 10/5/1/0, winner-spin-on-checkpoint + checkpoint excludes winners, 'r' save stamp), lib/party/party_models.dart (kWheelMiddle doc), lib/party/screens/round_ceremony.dart (mute +0 award chip), test/party_controller_test.dart, test/party/wheel_flow_test.dart (+ added gamePick cases to both test drive loops for the concurrent GAME RIGGER work) · verified: scoped analyze clean; both test files pass EXCEPT "freeze ray" + "buying a potato" which fail from the concurrent bazaar session's in-flight edits (not reachable by this diff — wheel-less, no minigame scoring) · dirty: none of mine; docs REQUEST above still open · note: concurrent session already adopted the rules stamp (kPartyRules now 4, bigBad gated rules ≥4) — compatible
- NOTE — root cause of Brett's "diamonds vanish between rounds": rules ≤ 3 _runOps robbed
  the diamond leader (min(diamonds,10) × 2 ops) at EVERY round boundary, and the turnLog
  line announcing it was wiped by the next roll() before any panel drew it. Fixed as the
  rules-4 prowl economy, not a patch.
- NOTE — fixed a latent save bug while verifying: toSaveJson/fromSaveJson dropped the
  gameMap, so any resumed GameMap match replayed onto the legacy board and desynced
  (repro was: save at any ceremony, fromSaveJson, chooseCardOption assert). Saves now
  carry 'map' (absent = legacy loop).
- REQUEST — docs/MAPS_SPEC.md (orchestrator): amend the ops/economy section: "Prowl
  economy (amended 2026-07-12, Brett, yumutsu): the mischief crew NEVER robs at round
  boundaries — banked diamonds persist. Ops relocate each round (tape). Every 4th round
  they PROWL (red ring + ⚠ on the board, warning chip on the roll panel): brushing an
  op's tile risks a coin-flip robbery (Masher: a potato if you hold one; Peeler/fallback:
  up to 8 💎), the crook slinks off carrying the loot, reclaimable by catching it. Strong
  Bond parries. Routing around the crew is the counterplay."
- REQUEST — docs/PARTY_CINEMATIC_SPEC.md §2 (orchestrator): wheel table gains a row:
  "| **Big Bad** | every 7th round, boss maps, before the mini-game reveal | the TRAILING
  seat stops it | decree table — LAST LOSES A 🥔 / THE GREAT REDISTRIBUTION (all diamonds
  pool; winner 50%, 2nd 25%, last 12.5%, the boss keeps the rest). The decree is announced
  on the game-reveal screen BEFORE play; its outcome renders on the round ceremony. A
  vote-skip fizzles the decree.|"
- DONE — files: party_controller.dart (ops/prowl/_opBrush/_robAttempt, _bigBadDue +
  decree wheel flow, _applyBossRule, skip fizzle, save 'map' field), party_models.dart
  (WheelTier.bigBad, boss WheelPrizeKinds, BossRule, kWheelBigBad, kOpsProwlEvery,
  kBigBadEvery), party_dialogue.dart (Butter decree beats), round_ceremony.dart (decree
  outcome panel), party_page.dart (surgical: prowl chip on roll panel, decree banner on
  intro, prowling op token styling), test/party/prowl_economy_test.dart (new, 7 tests)
  · verified: scoped analyze clean; prowl_economy_test 7/7; per-round save-replay probe
  all-OK then removed · dirty: none. gamePick switch errors in older party tests belong
  to the items-market session.

## party_page (_pathOption branch labels only)
- CLAIM — Brett screenshot bug: fork chooser shows two identical "PRESS ON" options on
  down_the_hole checkpoints. Cause: `delta <= 1` in _pathOption catches negative deltas,
  so backward forks (22→11 etc.) label as PRESS ON and BAIL OUT is dead code. Fix:
  `delta == 1`. One-line surgical edit to party_page.dart:1898 — no other files.
- DONE — files: party_page.dart (line 1898, `delta <= 1` → `delta == 1`) · verified:
  scoped analyze — only pre-existing use_super_parameters info at line 45, nothing new
  · dirty: none.

## fork-strategy (game_map placements + controller heat/branch-preview + _pathOption copy)
- CLAIM 20:10 — fork strategy pass, Down the Hole (Brett, yumutsu; TODO_strategy.md is the
  audit): (1) shop 29→24 so cut-through 14→25 genuinely skips the market (broke → cut
  ahead, rich → long diamond road + shelf); (2) BOILING VAT HEAT — ending your walk in the
  four deepest bands costs 1/2/3/4 💎 (Shack safe, Void Shield blocks), giving the bail-up
  checkpoints (22/44/66) a real reason to exist; gated **rules ≥ 5** (bumping kPartyRules
  4→5 — ready-up session: append your rev-5 semantics to the same doc entry per protocol);
  (3) controller `previewBranch` + `_pathOption` copy so the fork chooser NAMES the trade
  (PARTY UX LAW). Files: game_map.dart (shops set + heat helper), party_controller.dart
  (rules doc, _finishStep heat block, previewBranch near branchOptions), party_page.dart
  (_pathOption sub-copy only), docs/MAPS_SPEC.md (incl. the two open orchestrator REQUESTs:
  clock-out + prowl amendments), TODO_strategy.md, test/party/vat_heat_test.dart (new).
- NOTE — ready-up session landed rev 5 first; heat semantics APPENDED to the same rev-5
  doc entry per protocol (no double-bump). The 20:07 collision it flagged (previewBranch
  w/o BranchPreview = tree no-compile) was this session mid-edit — class landed minutes
  later; tree compiles.
- DONE 20:55 — files: game_map.dart (shops {29→24} + vatHeatFor + fork comments),
  party_controller.dart (rev-5 doc append, _finishStep heat block w/ Void Shield,
  previewBranch + BranchPreview), party_page.dart (_pathOption preview-driven copy: THE
  LONG WAY / CUT-THROUGH / BAIL UP / PRESS ON name the trade), docs/MAPS_SPEC.md (fork
  strategy section, shop 24, checkpoint line, + fulfilled BOTH open orchestrator
  REQUESTs: Shack clock-out amendment + prowl economy amendment — those REQUESTs are now
  CLOSED), TODO_strategy.md (statuses + bazaar-law supersedes affordability-gate
  analysis), test/party/game_map_test.dart (shops 24/58 + vatHeatFor band ramp),
  test/party/vat_heat_test.dart (NEW, 7 tests: heat bands, lose-stacking, Void Shield,
  rules≤4 heatless, cut/bail previews).
  · ALSO closed the two dangling casing reds flagged by three sessions (nobody claimed
  them): ghost_flow "release is announced" → matches Russ's de-shouted line; wheel_flow
  "freeze ray" → 'frozen solid'. Test-string-only edits.
  · verified: flutter analyze 0 errors; full party sweep (test/party/ +
  party_controller_test) 136/136 + party_net green. OPEN: Void/Aether fork TODOs in
  TODO_strategy.md (no code yet, by design — Brett confirms direction per map);
  PARTY_CINEMATIC_SPEC REQUESTs from the rules-v4 session remain open (not my claim).
  · dirty: none of mine.

## party online ready-up (controller readyUp/gate + net + passPhone screen + host autoStart)
- CLAIM 19:38 — mini-game READY CHECK (Brett): online rounds lock at passPhone until every
  seat readies (new logged input readyUp, pump gate rules ≥5, synchronized autoStart
  countdown). Spec: docs/READY_UP_SPEC.md (DRAFT — cyummu pending; implementation starts
  on yumutsu). Files: party_controller.dart (ready section only), party_actions.dart,
  party_net.dart (readyUp case), party_page.dart (passPhone case + appended
  _ReadyCheckScreen only), mini_game_host.dart (autoStart param), test/party/ready_up_test.dart.
- NOTE — rules-revision: spec bumps kPartyRules 4→5; if another session claims rev 5
  first, this feature appends its semantics to the same rev-5 doc entry (don't double-bump).
- DONE 20:10 — files: party_controller.dart (readyUp input kind appended, readySeats +
  readyUp(), skip-implies-ready in voteSkip, pump gate at passPhone, kPartyRules 4→5 +
  rev-5 doc entry, round-start/skip clears, _apply case), party_actions.dart (readyUp on
  interface + both impls), party_net.dart (readyUp validation case + miniScore now
  REJECTED during the ready check), party_page.dart (surgical: passPhone→_ReadyCheckScreen
  online, minigamePlaying→autoStart online + introAction offline-only, appended
  _ReadyCheckScreen), mini_game_host.dart (autoStart param), lib/dev/party_live_check.dart
  (driver readies seats), test/party/ready_up_test.dart (NEW, 13 tests),
  test/party_net_test.dart (both drivers ready up first). docs/READY_UP_SPEC.md LOCKED
  (yumutsu) + §2.4 host-enforcement amendment.
  · verified (pre-20:07): scoped analyze clean (only pre-existing use_super_parameters
  info); ready_up 13/13; party_net full suite green; full party sweep green EXCEPT the two
  known-red casing tests (ghost_flow announce + wheel_flow FROZEN — casing-sweep session,
  flagged above, unreachable by this diff).
  · dirty: none of mine.
- NOTE — COLLISION at 20:07: a live session saved party_controller.dart with a
  previewBranch()/BranchPreview fork-preview feature; `class BranchPreview` exists NOWHERE
  in lib/ yet, so the TREE DOES NOT COMPILE right now (board_play/ghost_flow/items tests
  fail at loading; game_map_test spiral-shops is that session's too). Not mine, not
  touched. That session also gates on rules >= 5 — append your semantics to the rev-5 doc
  entry in party_controller.dart (currently documents only the ready check; do NOT bump
  to 6 for heat).
- OPEN — live-RTDB harness run (flutter run -d chrome -t lib/dev/party_live_check.dart)
  blocked on the tree compiling again; run it in the integration QA pass.

## tools (catalog export — HPG frontend session)
- CLAIM 19:20 — new file only: test/tools/catalog_export_test.dart, a flutter-test-run
  exporter that writes build/cell-catalog.json (the hotpotatogames.com /play catalog
  contract: scales + games + slugs + ranks + accents). No shared-file edits; reads
  game_catalog/game_slug/bio_entity only. Part of the /explore-the-cell-sync skill
  (lives in the hotpotatogames repo).
- DONE 19:25 — files: test/tools/catalog_export_test.dart (new) · verified: test passes
  (125 games, 22 scales, all slugs round-trip) · dirty: none.
- CLOSED 20:16 — the OPEN harness item above: integrated tree compiles (BranchPreview
  landed), full party sweep green (walk_visual batch failure was a one-off flake, passes
  on re-run), live-RTDB harness PASS (room ZWHT, 4/4 clients, gameOver in 36 actions,
  converged — ready check exercised over the wire every round).

## party order+solo+tap-law (controller order/turn section, models items, ceremony/flair, page)
- CLAIM 20:38 — Brett yumutsu: (1) minimap out of the round ceremony (dialogs only);
  (2) tap-to-drive law: flair/ceremony/reveal auto-advance removed (ATTRACT excepted);
  (3) solo = human + 3 CPUs (auto-driven turns, fabricated mini-scores);
  (4) opening double-dice order ceremony → turnOrder (rules 6) + MULLIGAN reroll +
  QUEUE JUMPER ordinal swap items. Spec: docs/ORDER_AND_SOLO_SPEC.md (LOCKED).
  Files: party_controller.dart (order/turn-cycling + item effects), party_models.dart
  (2 appended PowerUps + catalog + solo playerCount), party_actions.dart, party_net.dart,
  party_page.dart (orderRoll screen + CPU driver, surgical), round_ceremony.dart,
  round_flair.dart, party_setup_page.dart, test/party/order_and_solo_test.dart.
- NOTE — bumping kPartyRules 5→6. Rev-5 semantics (ready check + heat) unchanged.
- DONE (2026-07-13 by this session, claim opened 20:38 on 07-12) — files:
  round_ceremony.dart (minimap backdrop + autoAdvance plumbing removed),
  round_flair.dart (beats hold for tap; auto only under ATTRACT),
  party_page.dart (reveal auto-dwell + _AutoAdvanceAfter removed; _OrderRollScreen +
  orderRoll cases; SOLO CPU driver + fabricated mini-scores; pass-phone solo copy),
  party_controller.dart (orderRoll phase+input+beginMatch, tie-group resolution →
  turnOrder, cursor turn cycling, kPartyRules 5→6 + rev doc, MULLIGAN reroll +
  QUEUE JUMPER queued swap, rules-gated shelf pools, isCpuSeat, TurnResult
  doubled/halved), party_models.dart (2 appended PowerUps + metadata + V6 pools +
  prices; solo playerCount 4, label YOU v 3 CPU), party_actions.dart + party_net.dart
  (rollForOrder/beginMatch + validations, reroll rollResult window),
  lib/dev/party_live_check.dart (ceremony wire case),
  test/party/order_and_solo_test.dart (NEW, 14 tests incl. tap-law widget checks),
  + fallout pass: orderRoll drive cases in 12 test files, pre-6 fixtures pinned to
  rules: 5 (house convention — seed-aligned), party_net drivers/out-of-turn test made
  order-aware, party_flow/smoke widget tests walk the ceremony.
  Spec: docs/ORDER_AND_SOLO_SPEC.md (LOCKED; prices amended 13/28 to fit ITEMS_SPEC
  bands, noted inline).
  · verified: FULL test suite 266/266 green; flutter analyze lib/party/ + test/ ZERO
  errors/warnings; live-RTDB harness re-run pending (in flight).
  · dirty: none.
