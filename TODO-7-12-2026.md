# TODOs — 7/12/2026 · Mini-game playtest feedback

> Format per item: complaint → resolution → the .dart where fine-tune adjustments live.
> Items honor `lib/games/GAME_DESIGN.md` (procedural Canvas only) and end with a clean
> `flutter analyze`. Append new games/items below as feedback comes in.

## Orbit Catch

- [x] **Target: yellow plus-circle → Earth** *(done 7/12 — procedural Earth with
  fixed continent layout, ice cap, drifting clouds, atmosphere rim; target read =
  gold rings pulsing INWARD onto the globe, crosshair gone; legible at the 13px
  floor; legend card updated)*
  > Complaint: the target is a yellow circle with a plus sign in it — ugly, reads as UI
  > chrome instead of an object in the world (the recent planet update looks really good;
  > the target lags behind it).
  > Resolution: render the target as an Earth-looking planet (procedural — blue oceans,
  > landmasses, atmosphere rim) with a clear in-world indication that it IS the target
  > (subtle pulsing beacon/ring that reads "destination", not a crosshair sticker).
  > Fine-tune: `lib/games/planets/orbit_catch/orbit_catch_game.dart`

- [x] **Launcher + projectile → rotating potato** *(done 7/12 — PotatoArt potato
  spins at 5.2 rad/s on the real dt, physics untouched on the circular 7px body;
  copper launch rail with the next potato visibly loaded + rocking at the muzzle;
  warm gold trail; legend cards + registry/catalog copy updated to the
  potato-at-Earth fiction)*
  > Complaint: the launcher and projectile need a rehash — placeholder pin/ball look,
  > doesn't match the new planet fidelity.
  > Resolution: the projectile becomes a rotating potato (use `PotatoArt` from
  > `lib/games/potato.dart`, the canonical renderer), spinning in flight; rework the
  > launcher visuals to match. VISUAL ONLY — the oblong shape does NOT change the
  > physics; collision/orbit math stays on the existing circular body. We are throwing
  > a potato at Earth.
  > Fine-tune: `lib/games/planets/orbit_catch/orbit_catch_game.dart` (+ read-only
  > `lib/games/potato.dart`)

- [x] **Planet surface blotches → real topology** *(done 7/12 — renderer extracted
  to shared `lib/games/planet_art.dart`; craters reworked as lit bowls: gradient
  floor, lit far wall, shadowed near wall, overlap rejection, small-skewed sizes)*
  > Complaint: some planets (GIANT / MID in the 7/12 screenshot) have blotchy darker
  > dots that look like stains, not interesting planet topology.
  > Resolution: shade the spots so they read as craters/terrain — rim highlight + inner
  > shadow, size/opacity variation, placement consistent with each planet's existing
  > radial-gradient light direction — instead of flat dark blobs stamped on top.
  > Fine-tune: `lib/games/planets/orbit_catch/orbit_catch_game.dart`

- [x] **Rubric gap (flagged while filing)** *(done 7/12 — GAME.md/AGENT.md/
  EDUCATION.md created at orbit_slingshot depth; GAME.md locks the direct-aim
  mapping, r^1.5 gravity rationale, 10-blueprint ladder, scoring, and the
  potato-is-visual-only law)*
  > Complaint: `lib/games/planets/orbit_catch/` has only the .dart — no GAME.md /
  > AGENT.md / EDUCATION.md, so the game can't count as complete under GAMES.
  > Resolution: whoever picks up the items above locks rules in `GAME.md` first
  > (rules are the asset; implementation is disposable), then adds the other docs
  > from `docs/templates/`.
  > Fine-tune: `lib/games/planets/orbit_catch/`

## Orbital Insertion

- [x] **Use the new Orbit Catch planet renderer** *(done 7/12 — extraction landed
  as `lib/games/planet_art.dart`; game + legend now render via PlanetArt)*
  > Complaint: Orbital Insertion's planets don't match the newly upgraded planet
  > visuals in Orbit Catch.
  > Resolution: render its bodies with the same rich procedural planet system
  > (`_PlanetSkin` / `_planetSkin` / `_paintPlanet`, currently PRIVATE inside
  > `orbit_catch_game.dart`). Prerequisite: extract that renderer into shared kit
  > (e.g. `lib/games/planet_art.dart`, self-contained like `potato.dart`) so both
  > games consume one painter — a shared-kit change, so the ORCHESTRATOR does the
  > extraction, not a per-game agent; Orbit Catch keeps identical visuals after.
  > Fine-tune: `lib/games/solar_systems/orbital_insertion/orbital_insertion_game.dart`
  > (+ extraction from `lib/games/planets/orbit_catch/orbit_catch_game.dart`)

- [x] **Redesign loop: continuous multi-moon orbiting** *(done 7/12 — "CONSTELLATION"
  v2 spec locked in GAME.md then reimplemented: zero-pause launching (0.25s gesture
  debounce only), capture +40+roundness, per-lap +25+roundness+5×aloft (ring-pulse =
  the stability heartbeat), collisions destroy both moons at −60 with 0.45s spawn
  grace, 12-moon fleet cap, escalation = mass swell → planet drift → debris. Agent's
  judgment numbers to playtest: −60 collision / +25 lap base. Analyze clean)*
  > Complaint: there's a pause after launching a moon into orbit — kills the flow.
  > The game would be more fun if it were dynamic: keep launching moons back-to-back.
  > Resolution: rules change (lock in GAME.md FIRST, then re-implement from spec):
  > goal = get as many moons into stable orbit SIMULTANEOUSLY as possible. No pause
  > between launches — launch freely while earlier moons keep orbiting. Moons have
  > collision mechanics: moon-vs-moon collisions deduct points (and presumably
  > destroy the moons). Every full revolution a moon completes earns points — the
  > indication that its orbit is stable — so score pressure = maximize concurrent
  > moon count vs. the collision risk of a crowded sky. Keep the ~60s host clock and
  > escalating difficulty per GAME_DESIGN.md.
  > Fine-tune: `lib/games/solar_systems/orbital_insertion/orbital_insertion_game.dart`
  > + `GAME.md` (spec first)

## NEW GAME — Terraform Rush (planets strata)

- [ ] **Build the core loop** *(dispatched to a subagent 7/12)*
  > Idea: rapid-fire WarioWare-style terraform microgame — "Seed the planet with
  > life!" Prompt (1s: "Make it Habitable!" / "Flood the Planet!" / "Stop the War!"
  > / "Don't ruin it!") → read the planet's VISUAL state (never numbers) → choose
  > the correct payload → deliver via one tiny mechanic → planet evolves (or not).
  > The real challenge is recognizing the correct intervention almost instantly,
  > not dexterity. Payload library: colony pod, fungus pod, forest capsule, ice
  > asteroid, atmosphere generator, moon, sun mirror, meteor, peace beacon, nanobot
  > cloud — plus the "do nothing" misdirection (thriving planet). Baseline delivery
  > mechanic (Brett): the pod must enter a SLIGHT orbit — dead-direct destroys the
  > pod/planet with the force; too much angle and it flies off into the aether.
  > One mechanic per mission, never more. Educational layer: greenhouse effect,
  > orbital mechanics, habitable zone, tectonics, moon stabilization, succession.
  > Resolution: new registry-style module `lib/games/planets/terraform_rush/` with
  > GAME.md locked first (full payload/archetype library in spec even if v1 ships a
  > subset), AGENT.md, EDUCATION.md, game widget on the MiniGameSession contract.
  > Registry + catalog wiring stays with the orchestrator.
  > Fine-tune: `lib/games/planets/terraform_rush/`
  > STATUS 7/12: module built by subagent (GAME.md/AGENT.md/EDUCATION.md/game.dart),
  > wired into registry + catalog, analyze clean. Remaining: playtest, tune
  > humanMax/starThresholds, v2 payloads (Sun Mirror rotate, Moon slingshot, etc.).

## Digest — `lib/games/organ_system/digest/`

Note: screenshot header reads "Digest" (v1). A `digest_v2` twin exists
(`lib/games/organ_system/digest_v2/`) — confirm which spec the current LEARN path
launches before working; apply the redesign to the one the player actually gets.
Current strategy critique (Brett): the whole game reduces to "look at the bottom,
when a button goes yellow tap it, rightmost first" — the redesign below adds real
per-organ mechanics so the tract is more than a glow-chase.

STATUS 7/12: ALL SEVEN DONE — twin confirmed v1 (`digest/`; the screenshot's banner
string is a verbatim v1 literal; `digest_v2` untouched). Full reimplementation to
the new GAME.md spec; legend carousel now 5 frames; autoPilot handles every new
mechanic (spits bad food, chews to depth, mashes chokes, douses spice, pulls the
flush). Choke = button-mash on ALL platforms (shake dropped deliberately: iOS
Safari DeviceMotion permission + patchy web support); SPIT is a floating tab under
the mouth morsel, not a 6th footer button. Analyze clean.

- [x] **Ingress visuals on organ handoff**
  > Complaint: no visual read on morsels entering the different systems — nodes just
  > sit in columns; the handoff between organs carries no feedback.
  > Resolution: give each morsel a visible transition when it crosses into an organ
  > (squeeze through the boundary, column lights up, organ icon reacts) so the tract
  > reads as a pipeline, not five static lanes.
  > Fine-tune: `lib/games/organ_system/digest/`

- [x] **Bad food: spit or suffer**
  > Complaint: no risk dimension — everything eaten is safe.
  > Resolution: some morsels are bad/sickening. Player can SPIT them out at the mouth
  > (reject mechanic); consuming one punishes (points loss / sick state that slows the
  > tract). Make bad food readable but increasingly subtle with difficulty.
  > Fine-tune: `lib/games/organ_system/digest/` + `GAME.md` (spec first)

- [x] **Chew depth: 1–3 taps**
  > Complaint: chewing is a single uniform tap.
  > Resolution: items require more thorough chewing — 1, 2, or 3 chew taps by food
  > toughness, with a visible remaining-chews indication on the morsel.
  > Fine-tune: `lib/games/organ_system/digest/` + `GAME.md`

- [x] **Choking event (swallow risk)**
  > Complaint: swallow has no failure mode.
  > Resolution: randomly, a swallow starts a CHOKE: shake the device to dislodge
  > (motion sensors, where available), else fallback = mash a button ~10x — feature-
  > detect motion support (web/mobile-web may not expose it; DeviceMotion needs a
  > permission prompt on iOS Safari); then drink water to wash it down. Fast, loud,
  > readable.
  > Fine-tune: `lib/games/organ_system/digest/` + `GAME.md`

- [x] **Spicy event (water ASAP)**
  > Complaint: —
  > Resolution: spicy morsels demand water IMMEDIATELY after they hit the tongue;
  > delay = escalating burn penalty. Water button already exists in the action row.
  > Fine-tune: `lib/games/organ_system/digest/` + `GAME.md`

- [x] **Flush handle after heavy churn**
  > Complaint: no payoff/relief valve for a full tract.
  > Resolution: after enough churn cycles (the being has eaten a lot), a handle
  > appears — pull it DOWN to "flush" (toilet flush), clearing the tract for bonus.
  > Fine-tune: `lib/games/organ_system/digest/` + `GAME.md`

- [x] **Organ column headers feel claustrophobic**
  > Complaint: the top row (MOUTH/ESOPHAGUS/STOMACH/SMALL INT./LARGE INT. + action
  > sublabels) is cramped — labels collide with icons and each other.
  > Resolution: spacing/typography pass on the column header band — smaller icon
  > footprint or stacked layout, breathing room between columns, don't fight the
  > oval backdrop.
  > Fine-tune: `lib/games/organ_system/digest/`

## Pre-game HOW TO PLAY screens — host card + ALL legend frames (93-game sweep)

Brett paged through the pre-game instruction screens (the `n/93` pager) — the legend
illustrations are systematically cramped. Two confirmed failure modes; audit all 93.
HOST-SIDE FIX DONE 7/12: the legend canvas is now screen-responsive (26% of screen
height, clamped 168–300px — was fixed 168px), giving every frame ~50%+ more room on
phones. REMAINING: the per-frame sweep below for painters that still collide/clip.

- [ ] **Crowded legend illustrations**
  > Complaint: instructions are crowded — e.g. Layer Builder (14/93): the ring
  > labels (Periderm / Cortex / Vasc. Ring / Pith) stack on top of each other inside
  > the concentric rings, unreadable. They all need more space.
  > Resolution: give the HOW TO PLAY illustration canvas more room in the host card,
  > and fix per-frame layouts so labels get callout leader lines / lateral placement
  > instead of stacking in the center. Consider a shared legend-layout helper so
  > frames can't collide by construction.
  > Fine-tune: `lib/games/mini_game_host.dart` (HOW TO PLAY card, ~lines 848/1774)
  > + each game's `*LegendFrames` list in its game.dart

- [ ] **Clipped legend content**
  > Complaint: buttons are cut off — e.g. Skin Layers (60/93): the DERM / FAT / EPI
  > tiles are chopped at the bottom edge of the illustration canvas.
  > Resolution: legend painters must respect the canvas bounds (a safe-area contract
  > for legend frames); host canvas height + per-frame scale so nothing draws past
  > the clip. Sweep all 93 games' frames for both crowding and clipping while in
  > there — screenshot-audit via ATTRACT/console paging if feasible.
  > Fine-tune: `lib/games/mini_game_host.dart` + per-game `*LegendFrames`

## Nephron — `lib/games/organ/nephron_v2/`

- [x] **Glomerulus header congestion** *(done 7/12 — in-canvas score stamp removed
  (host top bar already shows it; +N pops kept), rings 5→3 and ~43% smaller with
  outward fade, label moved clear of the rings; analyze clean)*
  > Complaint: the glomerulus at the top looks very congested/claustrophobic — the
  > big "214 molecules" score stamp overlaps the GLOMERULUS label and the emitter's
  > ripple rings; three text layers + rings occupy the same few hundred pixels.
  > Resolution: separate the layers — score readout belongs to the host HUD (it's
  > already in the top bar; drop or relocate the giant in-canvas counter), keep the
  > glomerulus emitter + its label with clear space, shrink/soften the ripple rings
  > so the tube's entry reads clean.
  > Fine-tune: `lib/games/organ/nephron_v2/`

## Circulate — `lib/games/organ_system/circulate_v2/` (confirm twin; v1 at `circulate/`)

- [x] **Bottom controls: inhale / exhale / heartbeat** *(done 7/12 — twin confirmed
  `circulate_v2`. Bottom row: HOLD INHALE fills the O₂ reserve, HOLD EXHALE vents
  CO₂ (a visible pressure that speeds organ drain past 60% — neglect costs),
  DOUBLE-TAP heart = lub-dub pump delivering to primed organs. Tapping a starving
  organ now PRIMES it; the beat moves the blood — the spawn loop Brett likes is
  untouched. In-world first-time prompts; autoPilot drives all three controls;
  scoring/thresholds unchanged; analyze clean)*
  > Complaint: current control layout doesn't sit at the bottom, and breathing is
  > one-dimensional (inhale only).
  > Resolution: bottom control row with three actions — HOLD to inhale (pull O₂ in,
  > as today), HOLD to exhale (expel CO₂ — yes, exhalation is how CO₂ leaves), and a
  > HEART node: after either breath, DOUBLE-TAP the heart to actually drive the
  > circulation. Keep the continuous spawning of oxygen-starved nodes that must be
  > tapped — that loop is good, don't touch it.
  > Fine-tune: `lib/games/organ_system/circulate_v2/` + `GAME.md` (spec first)

## Reflex — `lib/games/organ_system/reflex/` (confirm twin; Reflex Gate v2 at `reflex_v2/`)

- [x] **Moving nodes = stress** *(done 7/12 — confirmed twin `reflex/` (4-node arc
  w/ brain bypass; `reflex_v2` is the unrelated go/no-go gate). All 4 nodes drift on
  eased Lissajous paths: brain ~3% slow, cord ~1% tiny, receptor+muscle ~7% lively;
  amplitude scales with the trial ramp (55%→100%). Painter and tap sparks sample the
  same animated-geometry function; motion on the ticker canvas; analyze clean)*
  > Complaint: the focused node is static, and the brain-bypass moment misleads —
  > while waiting, the brain node feels like it's telling you to CLICK when its
  > actual message is BYPASS.
  > Resolution: all 4 nodes move, with motion budget by role — brain stays up top
  > but drifts slightly (a little lower/right/left/higher), spinal cord shifts just
  > a tiny bit, muscle and receptor move MUCH more dynamically. Goal: the game
  > should feel more stressful — that stress is desired. Moving targets also breaks
  > the "static node = tap prompt" misread on bypass.
  > Fine-tune: `lib/games/organ_system/reflex/`
