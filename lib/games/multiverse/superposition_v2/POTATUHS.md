# Superposition v2 — Potatuhs notes

Part of the **multiverseAll** scale set in Explore The Cell (cell_mobile), the
Summer hotpotatogames launch artifact. Sibling of `superposition`, built for the
UX Refinement Pass (Sprint 3) — same lesson, tuned for the Fun-Multiplayer bar.

- **Scale:** `BioScale.multiverseAll` — the outermost zoom, where the quantum
  multiverse games live (`branch`, `the_wait`, `superposition`).
- **Brand:** glaucous accent (`#7272AB`) carries the multiverse set; lock-zone
  green (`#7CFFB0`) is the "you nailed it" reward color. Atmosphere, orbs, pops
  and bursts come from the shared `GameFx` toolkit, so it sits flush with the
  rest of the catalog.
- **Voice hook (Russ):** "uhhh... is the cat alive or not? ...both, until you
  tap. Then you find out you should've waited half a second."

## GAMES rubric status
- **G** — `SuperpositionV2Game` widget, playable. ✓
- **A** — `AGENT.md` (this folder). ✓
- **M** — `GAME.md`. ✓
- **E** — `EDUCATION.md`. ✓
- **S** — host-driven session: closes on the host clock and re-enters clean
  (no internal timer; climax reads `session.remaining`). ✓
